export const sendMail = async (to, subject, recipient) => {
  // Simulate sending an email
  console.log(`Sending email to ${to} with subject "${subject}" and recipient "${recipient}"`);
  return Promise.resolve();
}